#!/usr/bin/env bash
# Run Shàn via the project virtual environment.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
PY="$ROOT/.venv/bin/python"

if [[ ! -x "$PY" ]]; then
  echo "error: $ROOT/.venv not found — run: ./scripts/bootstrap-venv.sh" >&2
  exit 1
fi

exec "$PY" -m shan "$@"
